{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug archivo actual",
            "program": "${file}",
            "cwd": "${workspaceFolder}"
        }
    ]
}
